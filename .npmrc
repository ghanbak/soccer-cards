# react-native-android-widget pulls @expo/ui's optional react-dom peer up to a
# version that wants a newer react than SDK 56 pins, so npm's strict peer
# resolution errors out. legacy-peer-deps lets installs (incl. EAS + worktrees)
# resolve the same tree the lockfile already captures.
legacy-peer-deps=true
